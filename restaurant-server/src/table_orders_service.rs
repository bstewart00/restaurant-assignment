pub trait TableOrdersService {}

pub struct TableOrdersServiceImpl {}

impl TableOrdersService for TableOrdersServiceImpl {
    // manipulates persistence and converts responses and errors to be client facing (view model, HTTP error statuses)
}
